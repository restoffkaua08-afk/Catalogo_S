name: Curar biblioteca de efeitos

on:
  push:
    paths:
      - '.github/focus-effects.trigger'

permissions:
  contents: write

jobs:
  curate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Aplicar curadoria
        run: |
          base64 -d .github/focus-effects.py.gz.b64 | gunzip > /tmp/focus_effects.py
          python /tmp/focus_effects.py
      - name: Validar biblioteca ativa
        run: |
          python - <<'PY'
          import json, pathlib
          root=pathlib.Path('.')
          data=json.load(open(root/'dados/efeitos.json', encoding='utf-8'))
          assert data['quantidade'] == 22
          assert set(x['familia'] for x in data['itens']) == {'Fundos e telas','Ícones','Sombras','Elevação no hover'}
          for x in data['itens']:
              p=root/x['caminho']
              assert p.exists(), p
              for name in ('bloco-pronto.html','bloco-pronto.txt'):
                  assert (p.parent/name).exists(), (p.parent/name)
          assert len([p for p in (root/'efeitos').iterdir() if p.is_dir()]) == 22
          print('22 efeitos focados validados')
          PY
      - name: Publicar e limpar infraestrutura temporária
        run: |
          rm -f .github/focus-effects.py.gz.b64 .github/focus-effects.trigger .github/workflows/focus-effects.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Curadoria: reduz efeitos e melhora demonstrações'
          git push